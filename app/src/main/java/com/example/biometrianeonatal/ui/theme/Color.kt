package com.example.biometrianeonatal.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primárias ───────────────────────────────────────
val PrimaryBlue = Color(0xFF2563EB)          // Azul principal da identidade visual.
val PrimaryBlueDark = Color(0xFF3B82F6)      // Variante mais clara para contraste no tema escuro.
val PrimaryContainerLight = Color(0xFFE0EDFF) // Fundo suave para cartões e destaques claros.
val PrimaryContainerDark = Color(0xFF1E3A6E)
val OnPrimaryContainerLight = Color(0xFF0A2463)
val OnPrimaryContainerDark = Color(0xFFD6E4FF)

// ── Secundárias ─────────────────────────────────────
val SecondaryTeal = Color(0xFF0891B2)        // Acento complementar para ações e indicadores.
val SecondaryTealDark = Color(0xFF22D3EE)
val SecondaryContainerLight = Color(0xFFE0F7FA)
val SecondaryContainerDark = Color(0xFF0E3E4A)
val OnSecondaryContainerLight = Color(0xFF064E5E)
val OnSecondaryContainerDark = Color(0xFFCCF5FF)

// ── Terciárias ──────────────────────────────────────
val TertiarySky = Color(0xFF0EA5E9)          // Tom de apoio para estados e diferenciação visual.
val TertiaryContainerLight = Color(0xFFE0F2FE)
val TertiaryContainerDark = Color(0xFF0C4A6E)
val OnTertiaryContainerLight = Color(0xFF0C4A6E)
val OnTertiaryContainerDark = Color(0xFFE0F2FE)

// ── Fundo e superfícies ─────────────────────────────
val BackgroundLight = Color(0xFFF8FAFC)      // Fundo quase branco com temperatura fria.
val BackgroundDark = Color(0xFF0F172A)
val SurfaceLight = Color(0xFFFFFFFF)         // Superfície branca para cartões e formulários.
val SurfaceDark = Color(0xFF1E293B)
val SurfaceContainerLight = Color(0xFFF1F5F9)   // Preenchimento discreto para agrupamentos.
val SurfaceContainerDark = Color(0xFF1E293B)

// ── Cores de conteúdo ───────────────────────────────
val OnPrimary = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF0F172A)            // Texto principal em alto contraste no tema claro.
val OnSurfaceDark = Color(0xFFE2E8F0)
val SurfaceVariantLight = Color(0xFFF1F5F9)  // Variante azul-acinzentada para componentes secundários.
val SurfaceVariantDark = Color(0xFF334155)
val OnSurfaceVariantLight = Color(0xFF64748B) // Texto de apoio com menor ênfase visual.
val OnSurfaceVariantDark = Color(0xFF94A3B8)

// ── Contornos ───────────────────────────────────────
val OutlineLight = Color(0xFFCBD5E1)         // Borda suave para campos e cartões.
val OutlineDark = Color(0xFF475569)
val OutlineVariantLight = Color(0xFFE2E8F0)  // Contorno ainda mais sutil para divisões leves.
val OutlineVariantDark = Color(0xFF334155)

// ── Erro ────────────────────────────────────────────
val ErrorLight = Color(0xFFDC2626)
val ErrorDark = Color(0xFFF87171)
val ErrorContainerLight = Color(0xFFFEE2E2)
val ErrorContainerDark = Color(0xFF450A0A)
val OnErrorContainerLight = Color(0xFF7F1D1D)
val OnErrorContainerDark = Color(0xFFFECACA)
